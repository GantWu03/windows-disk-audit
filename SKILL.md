---
name: windows-disk-audit
description: |
  审计 Windows 系统盘（通常为 C 盘）的空间占用，定位系统文件、Windows 维护数据、程序与安装缓存、用户目录、开发缓存、Docker/WSL/虚拟磁盘及未知对象，并输出“空间去向概览表 + 按真实目录解释用途、成因、处理选择和影响”的中文 Markdown 报告。用于“C盘满了”“查什么占空间”“哪些能清”“为什么又增长”“全盘空间排查”等请求。默认只读；建议仅供用户决策，未获得具体授权时不执行清理、卸载、停服务或系统配置变更。
metadata:
  author: Contributors
  version: "0.9.3"
  maturity: beta
---

# Windows 系统盘空间审计

目标是按目录解释空间去向，让用户理解每个对象的来源、用途和不同处理选择的后果，再由用户决定是否处理；不是机械填表，也不是替用户做清理决策。

## Router Rules

以下是不可突破的边界：

- 默认只读，审计与清理是两个任务。用户没有看到具体对象和动作并明确授权前，不删除、移动、卸载、停服务或改配置。
- 只检查用户允许的路径；不因权限不足取得所有权，不跟随重解析点跨出范围。
- `Cache`、`Temp`、`Logs`、旧日期、大体积或“可重新下载”都只是线索，不单独证明可删。
- 逻辑大小、实际占用、预计可回收和实际释放分开。未知、超时和权限失败不能写成 0。
- 系统组件、安装修复源、活动数据库、虚拟磁盘、项目、会话、备份和用户数据不能按普通缓存处理。
- 来源、用途或影响仍不明确时保留，并说明还缺什么证据。

这些是安全不变量。其余步骤是调查教程：可以根据机器现状、用户问题和证据调整顺序、工具、深度与表达，不必为了字段齐全调查低收益对象，也不应让模板限制进一步探索。

## Compact Workflow

一次只加载当前阶段需要的参考文件，避免把全部规则同时塞进执行上下文。

### 0. 定界和基线

读取 [工作流](references/workflow.md) 的阶段 0，并用 `scripts/new_audit_manifest.ps1` 记录目标盘、允许和排除范围、输出目录、时间预算与只读状态。随后运行 `scripts/collect_inventory.ps1 -Phase baseline`，记录系统盘容量、可用空间和根一级大头。

系统盘空间进入危险区时，Agent 不再启动新的下载、安装、大型复制、索引或无目标全盘扫描，并把报告及中间文件放到用户指定的非系统盘；未经授权不终止用户已有进程。脚本以 5 GiB 作为可调整的保守默认写入门禁，用户或环境可以设得更严格。

### 1. 形成短名单并规划分工

读取 [Windows 检查地图](references/windows-map.md)。先看与故障或异常增长相关的对象，再看预期空间收益大的目录；在收益接近时优先调查更可验证、可逆、低风险的对象。目录存在、名称或一次时间戳不足以入选。基线只提供通用入口，实际分工必须由本机出现的主要目录、特殊软件、未知对象和高风险类型决定，不能机械套用固定产品清单。

优先级是思考工具，不要求给所有发现贴 P0–P5：

- 紧急：正在异常增长、引发崩溃或系统盘极低空间。
- 高价值：多 GiB、用户关心，或处理方式风险较高。
- 普通候选：旧下载、转储、缓存、不再使用的软件或可迁移数据。
- 记录即可：低收益、明确保留或短期不影响决策的对象。

在启动子 Agent 前，先形成完整的 `cluster-plan`：列出每个目录簇的负责路径、边界、为何单独或合并、相关操作卡和产物位置。显著的大目录原则上一个子 Agent 负责；多个较小目录按生成者、用途或处理方式合并；同一应用分散在多个位置的数据尽量交给同一个子 Agent。只有大目录内部明显异质或需要特殊调查方法时才继续拆分。基线中达到本次“大目录”标准的每个一级对象必须明确分配给某个子 Agent；因权限、预算、排除范围或其他原因不能调查的，必须在计划中列为盲区并说明理由。分工以覆盖清楚为目标，不以增加 Agent 数量为目标。

### 2. 按计划并行调查

按 `cluster-plan` 启动互不重叠的子 Agent。只要软件允许且不违反只读、范围和低空间门禁，可以使用软件提供的并行上限，不在 Skill 中另设较低的固定并发数；执行环境无法并行时再按同一计划串行降级。

子 Agent 对其既定路径范围内的调查完整性、事实、依据、处理选择和影响解释负责。它在目录内发现新的重大对象时应继续调查，因为这本来就是其职责范围的一部分；不得因最初短名单未逐项列出而停止。只有目标落在其他 Agent 的负责路径或授权范围之外时才停止跨界，并在覆盖记录中标出。子 Agent 不执行清理，也不替用户作最终删除决定。

进入具体目录时读取 [目录级操作手册](references/directory-procedures.md) 的相关卡片。通常按“确认边界 → 一层拆分 → 深查大头 → 识别生成者和活动状态 → 判断用途与影响”推进；若专用工具或已有证据能更准确回答问题，可以调整方法并说明口径。

每个子 Agent或串行目录簇都提交 `findings.json`、`coverage.json` 和面向用户的 `chapter.md`。前两者保存候选与覆盖证据；`chapter.md` 直接按该簇的真实目录层级解释全部候选。每个有意义的对象都应让用户理解它是什么、为何出现、现在有哪些处理选择以及各选择会怎样；可以自然合并表达，不要求填写固定字段。子 Agent不能自行删去已经形成的候选，也不把尚未核实的推测包装成确定事实。

子 Agent先取得本机事实；当需要判断产品归属、官方维护方法、命令作用范围、删除影响或易变行为时，优先查找 Microsoft 或产品官方资料，并记录资料实际支持的结论。官方资料不存在、无法访问或没有回答当前问题时，再使用专用工具、可靠一手资料或明确标注的推断，并降低结论强度。低收益且不会改变处理判断的对象不要求为了形式联网。

### 3. 无损合并与矛盾裁决

子 Agent返回后，先运行 `scripts/merge_findings.ps1` 无损生成 `merged/findings.raw.json` 和覆盖记录，并保留各目录 `chapter.md`。合并阶段只建立来源、规范顺序和候选集合，不按重要性筛除。主 Agent不对所有章节重新调查或逐项事实复核；返回后的职责仅限于：

- 生成浅层空间概况；
- 修正父子目录重复计量和由后续子目录测量得到的已知下界；
- 合并真正相同的重复对象，并保留全部 `source_refs`；
- 识别并裁决不同子 Agent对同一对象产生的真实矛盾。

主 Agent不得因报告篇幅、重要性或表达偏好改写、摘要或压缩非重复、非冲突的子 Agent章节，也不替用户判断某个软件、项目、会话或备份是否还需要。每个原始候选必须保留最终去向，父目录结论不能覆盖内部已经识别出的异质对象。

只有不同子 Agent 对同一对象的事实、分类、计量或处理影响给出矛盾结论时，主 Agent才进入事实核实并作出裁决。裁决应查能直接支持或推翻冲突的本机证据、专用工具或官方资料，并在对应章节中说明裁决依据；没有矛盾时不重复调查。单个章节证据不足但没有跨章节冲突时，主 Agent应退回原子 Agent补充或保留其不确定性，不替它重新完成调查。

子 Agent在 `coverage.json`、`findings.json` 或 `chapter.md` 中承认的未知、抽样边界、权限失败和盲区必须原样进入最终报告。除非主 Agent因真实矛盾取得了足以解决该不确定性的新证据，否则不得把它改写为确定事实。

低收益和明确保留项可以较短，但仍需说明其基本用途以及为什么当前不值得处理。只有生成者、对象性质、处理方式和影响都相同的对象才能合并简写；不能用一个父目录结论吞掉异质子项。各子 Agent需要时读取 [证据与安全规则](references/evidence-safety.md)；调查 `%WINDIR%\Installer` 的子 Agent必须读取 [Windows Installer 缓存附注](references/windows-installer-cache.md)。

### 4. 补查小项和盲区

大头解释后，再按本次收益阈值检查较小单项，或按生成者聚合日志、转储、安装下载和构建缓存。100 MiB–1 GiB 只是常见起点，不是普适边界。补查形成的新候选由负责该范围的 Agent追加到完整集合；连续检查没有改变行动顺序、达到预算或权限错误过多时可以停止，但已有候选不得回删。

### 5. 写报告

读取 [报告指导](references/report-format.md)。最终 Markdown 正文直接使用各目录 `chapter.md` 或 `merged/chapters.raw.md`，主 Agent不得另写一份缩略正文。它只可定点删除真正重复的段落、修正父子计量、合入真实矛盾的裁决结果；其他章节文字原样保留。`findings.json` 是推荐的事实索引，报告含动作性建议或可回收数字时必须建立对应 decision index，具体见 [轻量记录约定](references/data-contracts.md)。`scripts/render_report.ps1` 只用于需要快速起草时，不要求最终报告与它逐字一致。

报告应让用户看懂空间去了哪里、每个对象为何存在、不同处理选择会怎样。开头先用一张“主要空间去向”表概述对象或目录、占用口径及其用途；表格只帮助定位，不把目录占用冒充可回收量。随后正文必须以真实目录为主线，在每个目录内按空间收益或调查顺序解释候选。通常包括：

- 审计范围、容量基线和限制；
- 主要空间去向概览表，至少让用户看到对象、位置或范围、占用和“它是什么”；
- 按目录组织的完整解释：位置与占用、用途和形成原因、可选处理方式、处理影响、关键依据及仍存疑点；
- 未检查范围和本次是否执行过清理。

在宣称“大头已经解释”前，每个目录簇的覆盖记录应说明已检查范围和限制；每个合并候选都应在最终结果中有 `source_refs` 和合并状态。完整性按实际候选集合检查，不要求固定覆盖百分比或固定数量。

除概览表和目录主线外，这些是信息目标，不是固定字段。相关内容可以用自然段合并表达；低收益或明确保留项可以简写，但不能只剩“保留”“卸载”“通过软件处理”等结论。默认不设置“最值得现在做的三件事”或行动排行榜；只有用户明确询问处理顺序，或系统盘已进入会影响稳定性的危险区时，才增加简短的紧急提示，而且不能代替目录正文。不要输出几十行同构字段，也不要用“需要逐项授权”“恢复方式：自动重建”等内部套话代替前因后果。

可运行 `scripts/validate_report.ps1` 检查范围、只读状态和危险结论等硬门禁；验证器不评价文风，也不要求固定章节或确定性全文。

### 6. 独立清理任务

只有用户看过具体候选并明确授权后才执行。重新核验精确路径、活动状态、动作范围、影响、预计收益和恢复条件；优先使用 Windows、应用、包管理器或虚拟化工具支持的方式。小批量执行后测量实际释放并验证功能。

## Gate Ladder

- G0 范围：允许和排除路径、只读状态及安全输出位置明确；基线中的一级大目录均已分配或列为盲区后才启动子 Agent。
- G1 计量：部分结果、权限失败、父子重复和逻辑/物理/可回收口径没有混用。
- G2 候选闭合：合并集合中的每个 `source_ref` 都进入最终合并结果；子 Agent声明的盲区保持不确定；动作性建议与可回收数字进入 decision index，未知动作值直接失败。
- G3 执行：只有用户看到具体对象和影响并另行授权，才进入状态变更任务。

这些门禁只保护权限、事实和数据安全，不规定调查工具、候选数量、联网次数或报告版式。

## Output Contract

- `audit-manifest.json`：范围、授权和预算。
- `cluster-plan.json`：基于本机基线、在启动子 Agent前确定的目录簇、负责路径、分组理由和输出位置。
- `baseline\`、`clusters\`：原始只读证据；每个目录簇含 `findings.json`、`coverage.json` 和 `chapter.md`。
- `merged\findings.raw.json`、`coverage.raw.json`、`chapters.raw.md`：子 Agent候选、覆盖记录和目录章节的无损合并结果。
- `final\findings.json`：无损合并、去重和必要矛盾裁决后的完整候选集合；允许合并重复但不得漏掉来源。
- 各目录簇 `chapter.md`：子 Agent直接写成面向用户的目录解释章节。
- 最终 Markdown：开头由主 Agent生成空间去向概览表；正文直接使用子 Agent章节，只允许删除真正重复、修正父子计量和合入真实矛盾裁决。
- 公开分享前脱敏用户名、账号、项目、私人文件名、哈希和设备标识。

可选工具：[`new_audit_manifest.ps1`](scripts/new_audit_manifest.ps1)、[`collect_inventory.ps1`](scripts/collect_inventory.ps1)、[`render_report.ps1`](scripts/render_report.ps1)、[`validate_report.ps1`](scripts/validate_report.ps1)。

## 不适用与降级

不用于 RAM、云盘配额、非 Windows 平台审计，或一个已知普通文件的直接删除。权限不足、联网失败、超时或证据冲突时，交付部分报告并明确边界；不要为追求“完整”无限重扫或猜测。
